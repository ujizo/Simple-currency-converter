# 💱 Simple Currency Converter - Руководство по запуску

**Полная инструкция по компиляции и запуску конвертера валют на Windows**

---

## 📋 СОДЕРЖАНИЕ
- [Требования](#-требования)
- [Быстрый старт за 2 минуты](#-быстрый-старт-за-2-минуты)
- [Подробная установка](#-подробная-установка)
- [Компиляция проекта](#-компиляция-проекта)
- [Исправление ошибок](#-исправление-ошибок)
- [Использование](#-использование)
- [Часто задаваемые вопросы](#-часто-задаваемые-вопросы)

---

## ✅ ТРЕБОВАНИЯ

| Компонент | Где взять |
|---------|---------|
| Windows 7/8/10/11 | Уже есть |
| MSYS2 MinGW 64-bit | [msys2.org](https://www.msys2.org/) |
| Интернет | Для установки пакетов и получения курсов валют |

---

## ⚡ БЫСТРЫЙ СТАРТ ЗА 2 МИНУТЫ

### Шаг 1. Установите MSYS2
1. Скачайте с [msys2.org](https://www.msys2.org/)
2. Установите в `C:\msys64`
3. Запустите **MSYS2 MSYS**
4. Выполните обновление:
   ```bash
   pacman -Syu
   ```
   (после обновления **закройте и снова откройте** терминал)

### Шаг 2. Установите всё необходимое
В **MSYS2 MinGW 64-bit** (важно! не в MSYS) выполните:
```bash
pacman -S --needed mingw-w64-x86_64-toolchain mingw-w64-x86_64-cmake mingw-w64-x86_64-curl mingw-w64-x86_64-nlohmann-json
```

### Шаг 3. Скачайте и скомпилируйте
```bash
git clone https://github.com/ujizo/Simple-currency-converter.git
cd Simple-currency-converter
g++ -std=c++11 *.cpp -o converter.exe -lcurl
cp /mingw64/bin/libcurl-4.dll ./
```

### Шаг 4. Запустите!
```bash
./converter.exe
```

---

## 📦 ПОДРОБНАЯ УСТАНОВКА

### 1. Установка MSYS2
1. Скачайте установщик с [официального сайта](https://www.msys2.org/)
2. Запустите и установите в `C:\msys64`
3. Запустите **MSYS2 MSYS** из меню Пуск
4. Выполните:
   ```bash
   pacman -Syu
   ```
5. **Закройте терминал** (обязательно!)
6. Снова откройте **MSYS2 MSYS**
7. Выполните финальное обновление:
   ```bash
   pacman -Su
   ```

### 2. Установка компилятора и библиотек
**ВАЖНО:** Запустите **MSYS2 MinGW 64-bit** (это другой ярлык!)

```bash
pacman -S mingw-w64-x86_64-gcc
pacman -S mingw-w64-x86_64-curl
pacman -S mingw-w64-x86_64-nlohmann-json
pacman -S mingw-w64-x86_64-cmake  # опционально
```

Проверьте установку:
```bash
g++ --version
curl --version
```

---

## 🔧 КОМПИЛЯЦИЯ ПРОЕКТА

### Способ 1. Простая компиляция (рекомендуется)
```bash
git clone https://github.com/ujizo/Simple-currency-converter.git
cd Simple-currency-converter
g++ -std=c++11 main.cpp converter.cpp api.cpp cache.cpp -o converter.exe -lcurl
```

### Способ 2. Компиляция всех cpp файлов
```bash
g++ -std=c++11 *.cpp -o converter.exe -lcurl
```

### Способ 3. Статическая компиляция (не требует DLL)
```bash
g++ -std=c++11 *.cpp -o converter.exe -static -lcurl -lws2_32 -lbcrypt -lwldap32 -lz
```

---

## 🚨 ИСПРАВЛЕНИЕ ОШИБОК

### Ошибка: `libcurl-4.dll не найден`
**Решение:** Скопируйте DLL в папку с программой
```bash
cp /mingw64/bin/libcurl-4.dll ./
```

### Ошибка: `Problem with the SSL CA cert`
**Решение:** Отключите проверку SSL в `api.cpp`
```cpp
curl_easy_setopt(curl, CURLOPT_SSL_VERIFYPEER, 0L);
curl_easy_setopt(curl, CURLOPT_SSL_VERIFYHOST, 0L);
```
После исправления перекомпилируйте.

### Ошибка: `nlohmann/json.hpp: No such file`
**Решение:** Установите библиотеку JSON
```bash
pacman -S mingw-w64-x86_64-nlohmann-json
```

### Ошибка: `g++: command not found`
**Решение:** Запустите **MSYS2 MinGW 64-bit**, а не MSYS2 MSYS!

---

## 💻 ИСПОЛЬЗОВАНИЕ

### Запуск программы
```bash
cd Simple-currency-converter
./converter.exe
```

### Пример работы
```
From (USD/EUR/RUB etc): USD
To (USD/EUR/RUB etc): RUB
Amount: 100
100 USD = 9125.5 RUB
```

### Доступные валюты
Программа поддерживает **все мировые валюты** через API:
- USD, EUR, RUB, GBP, JPY, CNY
- И любые другие по коду ISO 4217

---

## ❓ ЧАСТО ЗАДАВАЕМЫЕ ВОПРОСЫ

### Почему программа пишет "Conversion failed!"?
Вы ввели несуществующий код валюты. Проверьте правильность: USD, EUR, RUB и т.д.

### Почему не работает интернет?
1. Проверьте SSL сертификаты (отключите проверку)
2. Используйте HTTP вместо HTTPS
3. Проверьте firewall

### Можно ли запустить без MSYS2?
Да, скопируйте `converter.exe` и `libcurl-4.dll` на любой компьютер с Windows.

### Как обновить курсы валют?
Программа получает **актуальные курсы** при каждом запросе. Интернет обязателен.

### Программа работает медленно?
Это нормально — выполняется HTTP-запрос к API. Обычно 1-2 секунды.

---

## 📁 СТРУКТУРА ПРОЕКТА

```
Simple-currency-converter/
├── main.cpp          # Главный файл
├── converter.cpp     # Логика конвертации
├── converter.h       
├── api.cpp          # Работа с API валют
├── api.h            
├── cache.cpp        # Кэширование курсов
├── cache.h          
├── utils.h          # Вспомогательные функции
└── converter.exe    # Скомпилированная программа
```

---

## 🎯 ПРОВЕРКА РАБОТОСПОСОБНОСТИ

После запуска программа должна:
1. ✅ Запросить исходную валюту
2. ✅ Запросить целевую валюту  
3. ✅ Запросить сумму
4. ✅ Показать конвертированную сумму
5. ✅ Предложить продолжить или выйти

---

## 🆘 ПОЛУЧЕНИЕ ПОМОЩИ

Если возникли проблемы:
1. Перечитайте инструкцию
2. Проверьте, что используете **MSYS2 MinGW 64-bit**
3. Убедитесь, что все пакеты установлены
4. Создайте Issue на GitHub проекта

---

**Удачи с конвертером!** 💰🔄💶

---

*README составлен на основе реального опыта компиляции проекта 12 февраля 2026 года*
